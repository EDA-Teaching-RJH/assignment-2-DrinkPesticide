def main():
    numbers = [3, 1, 4, 1, 5, 9, 2, 6, 5, 3]
    numbers.sort()
    # sorts the list in ascending order
    print(numbers)
    # remove number if it is 1
    # Adds numbers 7 and 8 at the end of the list.
    numbers.extend()
    # prints the finals list
main()
